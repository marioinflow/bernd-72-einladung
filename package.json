{
  "name": "bernd-72-einladung",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "dev": "npm run build && node scripts/serve.mjs",
    "check": "node --check scripts/build.mjs && node --check scripts/serve.mjs && node --check app.js && npm run build"
  },
  "engines": { "node": ">=20" }
}
